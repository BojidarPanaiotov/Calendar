<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{{title}}}</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/utils.css">
</head>

<body>
    {{{body}}}
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        /* Backdrop styling */
        .backdrop {
            display: none;
            /* Hidden by default */
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            /* Semi-transparent background */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Modal styling */
        .modal {
            background: white;
            padding: 20px;
            border-radius: 8px;
            width: 300px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        /* Button styling */
        button {
            padding: 10px 20px;
            border: none;
            background: #007bff;
            color: white;
            cursor: pointer;
            border-radius: 4px;
            font-size: 16px;
        }

        button:hover {
            background: #0056b3;
        }
    </style>
    <div id="modalBackdrop" class="backdrop d-none">
        <div class="modal">
            <h2>Modal Title</h2>
            <p class="content"></p>
            <button id="closeModalBtn">Close</button>
        </div>
    </div>
    <script src="/js/generateCalendar.js" type="module"></script>
    <script src="/js/modal.js" type="module"></script>
</body>

</html>